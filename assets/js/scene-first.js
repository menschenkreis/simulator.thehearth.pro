// Legacy placeholder.
//
// The active node entrances that used to live here now have named owners:
// Hearth, Practice, Play, Study, Create, and Mastery each live in adapters/.
// Keep this file loaded for now so old script-order checks remain calm while
// the prototype is being cleaned in small, reversible steps.
(function initSceneFirstLegacyPlaceholder(root) {
  "use strict";
  root.SceneFirst = root.SceneFirst || {};
})(typeof window !== "undefined" ? window : globalThis);
